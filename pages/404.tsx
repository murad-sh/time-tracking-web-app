import NotFound from '@/components/ui/NotFound';
import React from 'react';

const NotFoundPage = () => {
  return (
    <section>
      <NotFound />
    </section>
  );
};

export default NotFoundPage;
